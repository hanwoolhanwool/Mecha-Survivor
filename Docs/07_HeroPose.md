# 07 — 히어로 포즈 구현 계획서 (공중 런지 포즈)

작성: 2026-08-01. 레퍼런스: 사용자 제공 이미지
(`C:\Users\iam12\Downloads\ChatGPT Image 2026년 8월 1일 오전 03_48_01.png`).
제작 파이프라인·확정 설정은 **`05_MechaAnimation.md` §4-확정을 그대로 따른다** (여기서 반복하지 않음).

**상태: A안 P0~P1 완료 (2026-08-01)** — 클립 제작·임포트·검증까지. §9에 결과와 잔여 결정 사항.

---

## 1. 목적

레퍼런스 이미지의 **공중 런지(도약 사격) 포즈**를 주인공 기체(Mecha.fbx)로 재현한다.
1차 산출물은 **포즈 클립 1개**이고, 어디에 쓸지(전시용 vs 인게임)는 §3에서 결정한다.

## 2. 포즈 분석 (레퍼런스 이미지 분해)

전신이 공중에 떠 있고, 상체를 앞으로 숙이며 좌측(총구 방향)으로 비튼 도약 자세.

| 부위 | 자세 | Blender 키잉 포인트 (전방 -Y 기준) |
|---|---|---|
| Hips (전신) | 공중 부양 + 전경(前傾) 약 20~25° + 좌측 비틀림 | Hips 이동 +Z(상승) / +X축 회전(전경) / Z축 요 비틀림. 루트 본은 원점 고정 |
| 척추/가슴 | 전경을 이어받아 추가 굽힘 + 좌회전(총구 쪽으로) | Spine~Chest에 트위스트 분산 |
| 머리 | 총구 방향(좌하단)을 응시 | Neck+Head 좌회전, 약간 숙임 |
| 오른팔 | 팔꿈치 굽혀 몸통 앞을 가로질러 소총 파지, 총구는 좌하단 | 오른손이 몸 중앙~좌측으로. **손의 위치·회전만 잡으면 레일건은 RigProfile로 따라옴** (§4 참고) |
| 왼팔 | 뒤-바깥쪽으로 크게 뻗어 균형, 손바닥 펼침 | 어깨 신전 + 외전. ※ 리그 24본 — 손가락 본 없음, "펼친 손"은 표현 불가(한계로 수용) |
| 오른다리 | 무릎을 강하게 접어 발이 뒤로 들림 (접은 다리) | 고관절 굴곡 + 무릎 최대 굴곡 |
| 왼다리 | 아래-뒤로 곧게 뻗음 (진행 반대 방향) | 고관절 신전, 무릎 거의 폄, 발끝 포인트 |

핵심 실루엣: **"오른다리 접고 왼다리 뻗은 비대칭 X자"** + **총구와 시선이 같은 방향(좌하단)**.
이 두 가지가 살면 나머지는 미세 조정.

## 3. 활용처 — 결정 사항

| 안 | 내용 | 추가 작업량 |
|---|---|---|
| **A. 전시용 히어로 포즈 (권장 1차)** | 미세한 부유 사웨이(±5cm, 2~4s 루프)를 얹은 정적 포즈 클립. RigLab/격납고/타이틀 연출·스크린샷용 | 클립 1개 + 확인용 배선만. 게임 코드 무변경 |
| B. 인게임 대쉬/점프 액션 | 기존 대쉬 8클립(0.4s)의 절정 프레임을 이 포즈로 교체하거나, 점프 전용 상태 신설 | AC_Mecha 전환·드라이버 수정 + EditMode 테스트 필요 |

**권장 진행**: A를 먼저 완성(P0~P1)해 포즈 품질을 확정하고, B는 결과를 보고 별도 결정.
포즈 액션은 공용이므로 A→B 확장 시 재제작 없음.

## 4. 제작 방식 — 핵심 결정

- **소스**: `Mecha Ver 2.blend`의 `Riging_Meshy` 아마추어에 새 Action
  **`Mecha_Pose_HeroLunge`** (30fps, 2s=60프레임 루프, `use_fake_user=True` 보존).
  - 프레임 0 = 완성 포즈. 루프 구간 전체가 포즈 유지 + 미세 사웨이(Hips ±5cm, 팔다리 ±2° 남짓).
- **소총 문제**: 모델에 소총 메시가 없다. 오른손 장착 레일건(`Mount_RightHandWeapon`,
  RigProfile_Mecha)이 소총 역할 — **포즈에서 오른손 트랜스폼만 정확히 잡으면 무기는
  본을 따라온다.** 총구 각도가 어색하면 RigLab에서 마운트 로컬값 조정(씬 편집 금지, [[riglab-status]]).
- **루트 모션 금지 유지**: 공중 부양감은 Hips +Z 이동으로만. 루트 본 원점 고정.
- **내보내기/임포트**: `Rigged/Mecha_Anim_HeroLunge.fbx` (아마추어만),
  05 §4-확정 설정 그대로 (`FBX_SCALE_ALL` ★, 씬 프레임 범위=액션 범위 ★,
  Humanoid + CopyFromOther + Bake Into Pose 전부).

## 5. 단계

### P0 — Blender 포즈 제작 + 내보내기
1. §2 표대로 키잉 → 뷰포트 4방향 캡처로 레퍼런스와 실루엣 비교(특히 X자 다리·총구/시선 일치).
2. 미세 사웨이 키 추가(0/30/60프레임, 60=0 복사로 루프 봉합).
3. `scene.frame_start/end`를 액션 범위에 맞춘 뒤 FBX 내보내기.
   **잡 실행 중 Unity 수동 조작 금지** ([[meshy-model-pipeline]]).

### P1 — Unity 임포트 + 검증 배선 (A안)
1. `refresh_unity` → 임포트 설정(execute_code, 05 §4-확정 코드) → 콘솔 에러 0 확인.
2. 확인 배선은 **RigLab.unity 재사용**: RigLab의 프리뷰 Animator에서 클립 재생
   (Animator `AlwaysAnimate` 유지 — 게임 뷰 미렌더 시 포즈 멈춤 함정).
   에디트 모드에서 눕는 건 정상, **판정은 Play에서**.
3. 레퍼런스 이미지와 같은 앵글로 스크린샷 → 나란히 비교 → 어긋난 부위는 P0로 돌아가 수정
   (반복 1~3회 예상).
4. 필요 시 격납고/타이틀 노출 배선 — 사용자 확인 후.

### P2 — (보류) 인게임 액션화 (B안)
사용자가 A 결과를 보고 결정. 착수 시 05 §10 백로그 형식으로 사양을 먼저 쓴다
(대쉬 교체라면 스냅 파라미터·전환 순서 함정 — 05 P5 절 참조).

## 6. 검증 (Definition of Done)

- CLAUDE.md §1 3단계: `dotnet build` 오류 0 / `read_console` 에러 0 / 기존 테스트 전부 통과.
  (A안은 신규 코드 없음 — 기존 테스트 회귀 확인만. B안 착수 시 드라이버 로직 EditMode 테스트 필수.)
- 시각 판정: Play 모드 스크린샷 vs 레퍼런스 이미지 — 실루엣 3요소
  (①비대칭 X자 다리 ②전경+좌비틀림 상체 ③총구·시선 방향 일치) 체크.

## 7. 리스크 / 알려진 한계

| 항목 | 내용 | 대응 |
|---|---|---|
| 손가락 표현 불가 | 24본 리그, 손가락 본 없음 → 왼손 "펼침" 재현 불가 | 손목 각도로 뉘앙스만. 수용 |
| Humanoid 리타게팅 뒤틀림 | 극단 포즈(무릎 최대 굴곡)에서 근육 공간 클램프 가능 | ✅ 실측 결과 왜곡 없음 (무릎 굴곡 ~105° 그대로 재현) |
| 오른손-총 정렬 | 손 위치는 애니, 총 각도는 RigProfile — 두 소스 | ✅ 현 RigProfile 값 그대로 총구가 좌하단(기체 오른쪽-전방-하향) — 조정 불필요 |
| 스케일/프레임 함정 | 05 §4-확정의 ★ 2건 (재발 이력 있음) | ✅ 내보내기 스크립트에 명시 포함, 재발 없음 |
| **Root 본 함정 (신규 발견)** | 아마추어에 `Root` 본이 있으면 Humanoid CopyFromOther가 **조용히 실패**(클립 0개, 콘솔 에러 없음) | §9-3 절차 — 내보내기 전용 복제 리그에서 Root 제거 + `use_armature_deform_only=True`. 05 §4-확정에도 ★로 기록 |

---

## 8. 제작 규약 — 포즈 지정 방식 (재작업 시 그대로 사용)

본 로컬 축을 추측하지 않고 **본별 절대 월드 방향**으로 지정했다. 헬퍼는 아래 한 줄이 핵심이다.

```python
# 본의 head→tail 축을 월드 방향 d로 정렬 (twist = d축 기준 추가 회전)
q = rest_dir.rotation_difference(d) @ R_rest          # R_rest = bone.matrix_local.to_quaternion()
if twist: q = Quaternion(d, radians(twist)) @ q
pose_bone.matrix = Matrix.Translation(head) @ q.to_matrix().to_4x4()   # 부모 먼저, 매 본마다 view_layer.update()
```

- 좌표 규약(Blender 아마추어 공간): **+X=기체 왼쪽 / −Y=전방 / +Z=위**.
  `limbdir(az, el)` — 팔다리 (az=0 전방, +90 기체 왼쪽, el=−90 아래).
  `updir(pitch, roll)` — 척추/골반/머리 (pitch=+ 앞으로 숙임, roll=+ 기체 왼쪽으로 기울임).
- 처음엔 "본별 월드축 오일러 델타"로 시도했으나 **체인 누적(Δ_bone = Δ_parent @ δ)** 때문에
  팔이 의도와 다른 방향으로 올라가 제어가 불가능했다. 절대 방향 지정으로 바꾸고 나서야 수렴.
- 포즈 각도는 **레퍼런스의 관절 픽셀 좌표를 재고 단축(foreshortening) 보정으로 역산**해서 얻었다
  (화면상 길이 ÷ 실제 본 길이 = 코사인 → 카메라 축 성분). 눈대중 반복보다 훨씬 빨랐다.
- 포즈 스펙 원본: `scratchpad/heropose.py`(헬퍼) + `scratchpad/hero_action.py`(POSE_SPEC/SWAY).
  본 문서 §9-2에 확정 수치를 그대로 옮겨 두었다.
- **aim2 — 다리 필수 (2026-08-04 추가)**: aim()의 최소 회전은 본 **롤을 통제하지 않는다.**
  다리가 레스트와 가까운 방향(아래/뒤)일 땐 티가 안 나지만, 옆으로 쓸린 포즈에서는 허벅지
  장갑·무릎이 뒤틀려 보인다(반려 사유: "모든 다리가 부자연스럽다. 허벅지가 회전할 필요").
  다리 4본은 축 정렬 후 **본의 전면(레스트 월드 -Y)이 pole을 향하도록 추가 트위스트**한다.
  pole = normalize(허벅지방향 − 정강이방향) — §9-2 무릎캡 판정 방향 그 자체.

```python
# aim2: 축 정렬 + 전면 트위스트 (다리 본 전용, pole = 무릎캡 방향)
q_align = rest_dir.rotation_difference(d)
fw = q_align @ Vector((0,-1,0))                    # 레스트 전면이 정렬 후 가는 곳
ang = signed_angle(perp(fw, d), perp(pole, d), d)  # perp = d에 수직 사영, 부호는 d축 기준
q = (Quaternion(d, ang) @ q_align) @ R_rest
```

## 9. 결과 (2026-08-01)

### 9-1. 산출물

| 항목 | 값 |
|---|---|
| Blender 액션 | `Mecha_Pose_HeroLunge` (0~60프레임 / 30fps / 2s 루프, `use_fake_user`, `Mecha Ver 2.blend`) |
| FBX | `Assets/_Project/Art/Models/Mecha/Rigged/Mecha_Anim_HeroLunge.fbx` (아마추어만, 24본) |
| Unity 클립 | `Mecha_Pose_HeroLunge` — len 2.0s / 30fps / Loop / Human, Humanoid+CopyFromOther(MechaAvatar), Bake Into Pose 3종 |
| 사웨이 | Hips 월드 Y 0.382 → 0.333 → 0.382 (약 5cm 부유, 프레임 0=60 봉합 확인) |
| 루트 모션 | 없음 (root Y = 0 고정 확인) |

### 9-2. 확정 포즈 수치 (kind / a / b / twist)

`L`=limbdir(az, el), `U`=updir(pitch, roll). 프레임 30에서 사웨이 델타(±1~3°)만 더해진다.

```
Hips  U 20 -14  10   | Spine02 U 24 -15   4 | Spine01 U 30 -16  -3 | Spine U 36 -17 -9
neck  U 26 -14 -16   | Head    U 20 -12 -26
RightShoulder L -85   6 | RightArm L -55 -62 | RightForeArm L   5 -22 | RightHand L -12 -32
LeftShoulder  L  95   8 | LeftArm  L 100 -10 | LeftForeArm  L 118 -20 | LeftHand  L 124 -30
RightUpLeg L -48 -45 | RightLeg L 158  -8 | RightFoot L 168 -40 | RightToeBase L 168 -45
LeftUpLeg  L 155 -48 | LeftLeg  L 158 -36 | LeftFoot  L 158 -55 | LeftToeBase  L 158 -62
Hips location = (0, 0.02, +0.20) 월드 / 프레임 30에서 z −0.05
```

**pole 보정 소급 (2026-08-05, 사용자 승인)**: 다리 4본 중 UpLeg·Leg·Foot에 §8 aim2 +
양다리 이등분 pole을 소급 적용해 클립을 재생성했다 (트위스트 좌 18/18/18·우 23/34/46,
축 방향·사웨이·루프 봉합은 그대로). 무보정 원본은 blend에 `Mecha_Pose_HeroLunge_v1`로 백업.

왼다리는 2026-08-03 수정값이다. 초기값(UpLeg 130/−42, Leg 103/−58, Foot 108/−75, Toe 108/−80)은
① 다리가 뒤가 아니라 옆으로 벌어지고 ② 정강이 el이 허벅지보다 가팔라 **무릎이 뒤로 22° 꺾인
역관절**이었다(사용자 지적 "왼쪽다리가 돌아가 있다"). 판정 요령: 무릎캡 방향 ≈ normalize(허벅지방향
− 정강이방향)이 **전방-아래(−Y,−Z)** 를 향해야 정상. 트레일 다리는 정강이 el이 허벅지 el보다
**완만해야**(덜 음수) 자연 굴곡이 된다. 수정 시 프레임 30 사웨이는 기존 델타(본별 0.7~3.5°)를
쿼터니언 델타로 옮겨 보존했고, 60=0 봉합 유지.

### 9-3. 내보내기 절차 (★ Root 함정 포함 — 이대로 하지 않으면 클립이 안 생긴다)

1. 내보내기 전용 **복제 아마추어**를 만든다 (`src.data.copy()` → 새 오브젝트).
2. 복제본의 **모든 포즈 본 제약 제거** + `animation_data_clear()` (IK/CopyRot/DampedTrack이
   `Root["IK_*"]` 커스텀 프롭 드라이버에 의존하므로 Root를 지우면 에러가 난다).
3. EDIT 모드에서 **`Root` 본 삭제** → Hips가 최상위. `Mecha.fbx`의 MechaAvatar 계층
   (최상위=Hips, 24 디폼 본)과 일치시켜야 한다.
   - MCP에서 `bpy.ops.object.mode_set(mode='EDIT')`는 **컨텍스트 오버라이드 없이는 무시된다**
     (`armature.edit_bones`가 None으로 나옴). `bpy.context.temp_override(window=…, area=VIEW_3D,
     region=WINDOW, active_object=dup, selected_objects=[dup], object=dup)` 로 감쌀 것.
4. 복제본에 액션 연결(`animation_data.action` + `action_slot = act.slots[0]`) 후
   05 §4-확정 설정 + **`use_armature_deform_only=True`** 로 내보낸다.
5. 복제본 삭제 → 원본 리그 원상 복구 → blend 저장.

**증상 기억할 것**: Root가 남아 있으면 Generic으로는 테이크·클립이 정상이지만,
`animationType=Human` + `CopyFromOther`로 바꾸는 순간 **AnimationClip 서브에셋이 0개**가 되고
**콘솔에는 아무 에러도 안 뜬다.** 진단은 `importer.importedTakeInfos.Length`와
`LoadAllAssetsAtPath`의 Transform 개수 비교(정상=25, Root 포함=26)로 한다.

### 9-4. 검증 결과

- **실루엣 3요소** (§6 기준): ① 비대칭 X자 다리 ✅ ② 전경+비틀림 상체 ✅ ③ 총구·시선 일치 ✅
  — 레퍼런스와 동일 앵글 렌더 나란히 대조로 확인.
- **Play 모드 (RigLab)**: `PlayableGraph`로 클립을 프리뷰 Animator에 직접 물려 재생 확인.
  **레일건(오른손 마운트)·미사일 포드(등 마운트)가 포즈를 그대로 따라오고**, 총구가
  레퍼런스와 같은 좌하단 방향을 가리킨다. RigProfile 값 수정 없음.
- CLAUDE.md §1: `dotnet build` 오류 0 (경고 4 = 기존 MSB3277 노이즈) / `read_console` 에러 0 /
  EditMode 테스트 **255개 전부 통과**. A안은 신규 코드 0줄이라 회귀 확인 성격.
- 씬 변경 없음 (RigLab·Boot 모두 dirty 아님), 게임 에셋(AC_Mecha·RigProfile) 무변경.

### 9-5. 노출 배선 — RigLab 핫키 P ✅ (2026-08-01, 사용자 결정)

`AC_Mecha`에 상태를 만들지 않고 **RigLab에서만 `AnimationClipPlayable`을 Animator에 직접
물려** 재생한다. 확인용 클립으로 게임용 컨트롤러를 오염시키지 않기 위한 선택.

- **P**: 없음 → 포즈들 → 없음 순환. 우측 UI 패널에도 클립별 토글 버튼.
- 클립 목록은 `RigLabController._poseClips`(RigLab 씬 인스펙터). 앞으로 포즈가 늘면 배열에만 추가.
- 1~5 / 6~8 / 9·0 아무 키나 누르면 **포즈에서 자동 이탈**해 AC 상태로 복귀한다
  (그래프 파괴 → 컨트롤러 그래프 자동 복귀. `Animator.Rebind()`는 파라미터가 날아가서 안 쓴다).
- 공중 포즈는 루트 아래로 크게 뻗어 바닥에 파묻히므로 포즈 진입 시 **접지 오프셋을 다시 잰다**.
- `Animator.speed`는 PlayableGraph 출력에 안 걸린다 → `=/-` 속도는 플레이어블에 직접 준다.
- `OnDisable`·대상 전환 시 그래프 파괴 필수 (안 하면 Play 종료 시 누수 경고).
- 순환 규칙은 `RigLabMath.CycleWithNone`(신규 순수 함수, 마운트/총구 선택과 공용) — EditMode 3개 추가.

### 9-6. 잔여 결정 사항

1. **격납고/타이틀 노출**: 아직 안 함. 게임 화면에 히어로 포즈를 세우려면 별도 배선이 필요하다
   (RigLab은 에디터 전용 씬 — 빌드 미포함).
2. **B안 인게임 액션화** (§3·§5 P2): ✅ **대쉬 교체안 채택·완료 (2026-08-05)** —
   사양·구현은 05 §10-B7. 점프 전용 상태 신설안은 폐기(대쉬 교체로 목적 달성).

---

## 10. 방향별 히어로 런지 계획 (2026-08-03 작성 — **P0 v6 승인 (2026-08-04), P1 대기**)

### 10-0. 폐기 기록 — 단순 요 회전은 답이 아니다

2026-08-03 1차 시도(기준 포즈를 Hips 월드 Z 회전으로 L/R/B 복제)는 **사용자 반려로 전량
폐기**했다(FBX·액션·배선 모두 삭제, 산출물 없음). 요 회전 복제는 "방향"이 아니라 "카메라를
돌린 것"과 같다 — 몸짓이 전부 동일해서 방향성이 죽는다. **방향 변형은 방향마다 몸짓이
다른 별개 포즈로 제작한다.**

### 10-1. 핵심 컨셉 — "총을 든 채 이동하는 역동성"

기준 포즈(F)가 멋있는 이유를 분해하면 방향 변형의 설계 원칙이 나온다:

| 원칙 | 내용 |
|---|---|
| **총구·시선 고정** | 어느 방향으로 움직여도 **총구와 시선은 조준 방향(전방)을 유지**한다. 이동 방향과 조준 방향의 분리가 "전투 중 기동" 느낌의 핵심 |
| **체중 이동이 방향을 말한다** | 방향성은 회전이 아니라 **몸통 뱅킹(롤)·상체 비틀림·리드/트레일 다리 교체**로 표현 |
| **비대칭 X자 다리 유지** | 이동 방향 쪽 다리 접힘(리드) + 반대 다리 신전(트레일). §9-2 무릎캡 판정 요령 적용 |
| **좌우는 미러가 아니다** | 총이 오른손 고정이라 L과 R은 팔 구성이 근본적으로 다르다 — 별개 제작 |

### 10-2. 방향별 포즈 스펙 (초안 — P0에서 확정)

전 방향 공통: 공중 부양 + 2s 루프 사웨이(±5cm), 총구·시선은 전방 좌하단(기준 포즈와 동일),
루트 모션 금지.

| 방향 | 몸짓 콘셉트 | 키 포인트 |
|---|---|---|
| **F** (완성) | 전방 도약 사격 | 현행 유지 — §9-2 확정 수치 |
| **L** (좌 스트레이프) | 왼쪽으로 몸을 날리며 사격 유지 | 몸통 좌롤 ~20° + 상체는 **오른쪽으로 되비틀어** 총구 전방 유지. 왼다리 리드(풀 턱), 오른다리 트레일(오른쪽-아래 신전). 왼팔은 진행 방향(좌)으로 뻗어 밸런스 |
| **R** (우 스트레이프) | 오른쪽으로 몸을 날리며 사격 유지 | 몸통 우롤 ~20° + 상체 좌비틀림. 오른다리 리드, 왼다리 트레일(왼쪽-아래 신전). **총 든 오른팔이 진행 방향쪽** — 팔꿈치를 낮춰 겨드랑이를 연 채 총구만 전방 유지(L과 실루엣이 확연히 달라지는 지점). 왼팔 뒤-위 밸런스 |
| **B** (후퇴 런지) | 뒤로 몸을 던지며 견제 사격 | 상체 **후경 ~15°**(기준의 전경과 반대), 머리만 앞으로 숙여 시선 유지. 한 다리 무릎 접어 앞-위로 차올림, 반대 다리 아래로 신전. 총은 몸 중앙 견착 유지 — "빠지면서 쏘는" 실루엣 |

대각(FL/FR/BL/BR)은 4방 품질 확정 후 별도 결정 — 인접 방향 요소 조합으로 제작 가능성 있음.

### 10-2-1. P0 확정 수치 (2026-08-04 v6 승인 — 팔은 v5 값 + 트레일 다리 pole 오버라이드)

§9-2와 같은 형식 (kind / a / b / twist). **설계 핵심: §8 aim()은 본별 절대 월드 방향 지정이므로,
"총구·시선 고정"은 RightHand·Head 값을 F와 동일(근사)하게 두는 것만으로 달성된다** —
몸통이 어떻게 기울든 손·머리의 월드 방향은 부모 체인의 영향을 받지 않는다.

**v1 반려 기록 (2026-08-03)**: 1차 초안은 하체만 기울이고 척추에서 롤을 수평으로 되풀어
방향성이 약했다(사용자: "다리의 방향·기체의 각도·빈 팔이 방향에 맞게 자연스럽게").
v2 재설계 원칙 — **F를 일반화한다**: F가 "전방 이동 = 몸이 전방으로 기욺 + 다리가 뒤로
쓸림"이듯, ① 기체 전체가 이동 방향으로 기울고(롤을 가슴까지 유지: 하체 30° → 가슴 10°),
② 다리는 이동 **반대쪽**으로 쓸리며(트레일 신전 다리가 실루엣의 긴 대각선),
③ 빈 왼팔은 관성 랙으로 스윙, ④ Hips location도 이동 방향으로 치우침.
상체 조준(Head·RightHand)만 F 절대값으로 고정한다.

**L (좌 스트레이프)** — 기체 좌뱅킹(하체 roll 30·twist 22 → 가슴 10), 다리는 우측으로 쓸림,
왼팔은 F 기준으로 이동 방향 틴트만(수평 아래, 팔꿈치 접힘). 총 든 오른팔 체인은 F와 동일값.

```
Hips  U  10  30  22 | Spine02 U 14  24  12 | Spine01 U 18  17   2 | Spine U 22  10  -8
neck  U  24   0 -14 | Head    U 20 -10 -26                          (시선 ≈ F)
RightShoulder L -85   6 | RightArm L -55 -62 | RightForeArm L    5 -22 | RightHand L  -12 -32   (= F, 총구 고정)
LeftShoulder  L  96   8 | LeftArm  L 104 -25 | LeftForeArm  L  124 -35 | LeftHand  L  130 -45   (이동 방향 틴트, 반경 좁힘)
LeftUpLeg  L   62 -35 | LeftLeg  L -150  -8 | LeftFoot  L -160 -40 | LeftToeBase  L -160 -46   (리드 턱)
RightUpLeg L -112 -38 | RightLeg L -116 -26 | RightFoot L -116 -48 | RightToeBase L -116 -54   (트레일 — 우측 긴 대각선)
Hips location = (+0.10, 0.02, +0.20)
```

**R (우 스트레이프)** — 기체 우뱅킹(하체 -30·-20 → 가슴 -10), 다리는 좌측으로 쓸림. 오른팔은
팔꿈치를 낮춰 겨드랑이를 열고(Arm -75/-50) 손 방향만 F 근사(-15/-33)로 총구 유지.
빈 왼팔은 **좌로 스트리밍(수평 아래)** — 트레일 다리와 같은 흐름(몸은 우측, 팔다리는 좌측).
※ v2의 뒤-위 관성 랙(LeftArm 138/26)은 정면에서 단축돼 "뒤로 꺾인 팔"로 읽혀 반려(08-04).

```
Hips  U  10 -30 -20 | Spine02 U 14 -24 -10 | Spine01 U 18 -17   0 | Spine U 22 -10   8
neck  U  24  -4 -16 | Head    U 20 -12 -26                          (시선 = F)
RightShoulder L -85   2 | RightArm L -75 -50 | RightForeArm L  -25 -31 | RightHand L  -15 -33   (겨드랑이 염, 총구 근사 유지)
LeftShoulder  L  95   8 | LeftArm  L 113 -25 | LeftForeArm  L  133 -38 | LeftHand  L  139 -48   (좌 스트리밍, 반경 좁힘)
RightUpLeg L  -62 -35 | RightLeg L  145  -8 | RightFoot L  155 -40 | RightToeBase L  155 -46   (리드 턱)
LeftUpLeg  L  112 -38 | LeftLeg  L  116 -26 | LeftFoot  L  116 -48 | LeftToeBase  L  116 -54   (트레일 — 좌측 긴 대각선)
Hips location = (-0.10, 0.02, +0.20)
```

**B (후퇴 런지)** — 골반·척추 후경(-18~-8, v1보다 깊게), 머리만 전경(+18)으로 시선 유지.
다리는 이동 반대(앞)로 쓸림: 오른무릎 앞-위 차올림 + 왼다리 앞-아래 신전.
빈 왼팔은 앞으로 관성 스윙(수평). 총은 중앙 견착(= F 손).

```
Hips  U -18  -6   4 | Spine02 U -18  -8   0 | Spine01 U -14 -10  -6 | Spine U -8 -12 -10
neck  U   8 -11 -18 | Head    U  18 -11 -26                         (머리만 전경 — 시선 유지)
RightShoulder L -85   4 | RightArm L -45 -55 | RightForeArm L    8 -18 | RightHand L  -12 -32   (중앙 견착, 총구 = F)
LeftShoulder  L  92   8 | LeftArm  L  79 -20 | LeftForeArm  L   62 -32 | LeftHand  L   55 -42   (앞 관성 스윙, 반경 좁힘)
RightUpLeg L  -15 -12 | RightLeg L  178 -50 | RightFoot L  178 -70 | RightToeBase L  178 -75   (무릎 앞-위 차올림)
LeftUpLeg  L   10 -62 | LeftLeg  L  178 -80 | LeftFoot  L  178 -52 | LeftToeBase  L  178 -47   (앞-아래 신전, 발끝 포인트)
Hips location = (0, +0.12, +0.22)
```

- **무릎캡 판정(§9-2 요령) 전 방향 통과**: L = 좌(0.68,-0.69,-0.24)/우(0.36,-0.46,-0.82),
  R = 좌(-0.36,-0.46,-0.82)/우(-0.71,-0.66,-0.24), B = 좌(0.12,-0.98,0.16)/우(-0.16,-0.93,0.33)
  — 후방(+Y) 성분 없음. B 양무릎은 의도적으로 전방(차올림/랙).
- **다리 트위스트 v3 (2026-08-04)**: 위 스펙의 다리 4본 twist 열(0)은 무효 — 다리는 §8 aim2로
  적용한다(pole = 무릎캡 방향). aim2가 자동 산출한 트위스트(도, UpLeg/Leg/Foot 순, ToeBase=0):
  L 좌 (-27/-54/-65)·우 (-7/-11/-21), R 우 (32/55/71)·좌 (7/11/21),
  B 우 (18/12/17)·좌 (-10/-6/-13). 기록용일 뿐, 재현 시 pole 규칙이 자동 계산한다.
- **트레일 다리 pole 오버라이드 (2026-08-04, "오른쪽 다리 전체가 안으로 돌아가 있다" 반려)**:
  거의 편 트레일 다리는 이등분 pole이 **안쪽(내회전)** 을 향해 다리 전체가 돌아간다.
  스트레이프의 트레일 다리는 pole을 명시 지정 — **전방-아래+바깥**:
  L 오른다리 pole=(-0.30, -0.70, -0.55), R 왼다리 pole=(+0.30, -0.70, -0.55).
  굽힘이 큰 리드(턱) 다리는 이등분 pole 그대로.
  F에도 적용하면 좌 (18/18/18)·우 (23/34/46)으로 접은 무릎이 더 정돈됨
  (F는 양다리 이등분 pole — 트레일 오버라이드는 옆으로 쓸린 스트레이프 전용, 05 §10-B7 판별).
  → ✅ **F 클립 소급 적용 완료 (2026-08-05, 사용자 승인)** — §9-2 참조.
- **빈 왼팔 v4→v5 (2026-08-04)**: 수평 위로 올린 왼팔(el +22~+35)은 "너무 벌렸다"로 반려(v4에서
  F 기준 el -2~-24로 하강). v4도 "아직 팔이 너무 돌아가 있다, 반경 좁혀라"로 반려 →
  **v5: LeftArm el -20~-25까지 추가 하강**(손이 골반 높이, 팔꿈치 접힘). 위 스펙이 v5 값.
  경향 정리: 빈 팔은 몸통에서 멀리 뻗을수록 반려된다 — F의 붙은 팔이 상한선.
- 승인용 렌더는 리그에 실제 포즈를 적용해 촬영(키프레임·저장 없음, blend 무변경 확인).
  사웨이(±5cm/2s)는 F와 동일 방식으로 P1~P2 제작 시 부여.
- **작업 파일 확정 (2026-08-04 실측)**: 파일명과 달리
  **`Mecha Ver 2 [IK rig backup 0801].blend`가 현행 작업본이다** (08-04 저장, 08-03 왼다리
  수정 포함 — HeroLunge 프레임 0 실측 LeftUpLeg 155/-48 = §9-2 일치. 개틀링 건 컬렉션도 이 파일).
  `Mecha Ver 2.blend`는 08-01 09:37에 멈춘 **구본**(왼다리 수정 없음) — 열어서 작업하면 안 된다.
  현행본의 `Riging_Meshy`는 Root 없는 24본 디폼 전용 리그(제약 0) = Unity MechaAvatar와 일치,
  §9-3 Root 함정 절차 불필요. IK 작업 리그는 `Riging_Meshy_IK`(52본)로 별도 보관.

### 10-3. 단계 (방향당 사용자 확인 게이트)

1. **P0 — 스펙 확정**: 방향별 레퍼런스 확보(사용자 제공 또는 생성 이미지 승인) → §9-2 형식의
   kind/a/b/twist 수치 초안 작성. **사용자 확인 후 착수.**
   → v1(요 회전만)~v5 반려 이력은 §10-2-1 참조. **v6 승인 (2026-08-04)** — 뱅킹 유지 +
   트레일 다리 pole 오버라이드 + 빈 팔 반경 축소가 최종 스펙.
2. **P1 — L 1종 파일럿**: §8 규약(절대 월드 방향 지정)으로 제작 → 4방향 렌더 + 무릎캡/굴곡
   수치 검증(§9-2 요령) → **사용자 확인** (품질 기준 합의가 목적 — 반복 1~3회 예상).
   → **제작 완료 (2026-08-04)**: `Mecha_Pose_HeroLunge_L` (30fps, 0~60 루프, fake_user,
   fcurve 91개 = 22본 쿼터니언 + Hips location). 검증: 루프 봉합 f0==f60 OK / 사웨이 5.0cm
   (0.382→0.332, F와 동일 진폭) / 루트 모션 없음 / 무릎캡 전방. 현행본 blend 저장 완료.
   사웨이는 F의 SWAY 테이블 재사용, 다리는 매 프레임 aim2+pole(§10-2-1 오버라이드) 적용.
   ※ 렌더 함정: GameReady 컬렉션이 뷰 레이어에서 **exclude**돼 있으면 렌더가 빈 화면 —
   렌더 시에만 layer_collection.exclude=False로 잠시 켜고 원복할 것.
   결과 렌더 보관: `Docs/Renders/HeroPose/` (P1L_f0_* 4방향, P1L_f30_front 사웨이,
   P0v6_sheet_FLRB 승인 시트). → 사용자 "P2 진행" 지시(08-04)로 승인 갈음.
3. **P2 — R·B**: P1에서 합의된 기준으로 제작·검증.
   → **제작 완료 (2026-08-04)**: `Mecha_Pose_HeroLunge_R` / `_B` (30fps, 0~60 루프, fake_user,
   각 fcurve 91개 = 22본 쿼터니언 + Hips location). L과 동일 레시피(aim2+pole, F SWAY 재사용,
   R 트레일 왼다리는 §10-2-1 pole 오버라이드). 검증: 루프 봉합 f0==f60 OK / 사웨이 5.0cm
   (R 0.382→0.332, B 0.402→0.352 — B는 Hips z +0.22라 기준 높이만 다름) / 루트 모션 없음 /
   무릎캡·aim2 트위스트 전부 §10-2-1 기록값과 일치. 현행본 blend 저장 완료. Unity 무변경.
   렌더: P2R_f0_{front,front34,left34,rside}·P2R_f30_front,
   P2B_f0_{front,front34,rside,lside}·P2B_f30_front (제작 스크립트: 세션 scratchpad
   `hero_p2_rb.py` — §8 규약+§10-2-1 스펙으로 재작성한 것, 수치는 본 문서가 원본).
   **사용자 확인 대기.**
4. **P3 — 임포트·배선**: §9-3 익스포트 → 기존 임포트 설정 복제 → RigLab `_poseClips`
   [F, L, R, B] 배선 (코드 무변경).
   → **완료 (2026-08-05)**: `Rigged/Mecha_Anim_HeroLunge_{L,R,B}.fbx` 익스포트
   (현행본 리그는 Root 없는 24본 디폼 전용이라 §9-3 복제 리그 절차 생략, 05 §4-확정
   설정 그대로 직접 익스포트). 임포트는 Human+CopyFromOther(MechaAvatar)+Bake Into Pose
   3종+Loop — 4종 전부 클립 1개/Transform 25/2.0s/Human/Loop 확인 (Root 함정 통과).
   RigLab `_poseClips` = [F, L, R, B] 배선 후 씬 저장, 코드 무변경. Play 스모크:
   L·R·B 재생 + 마운트 무기 추종 확인 (스크린샷 `Docs/Renders/HeroPose/P3_unity_{L,R,B}.png`).
   검증: 빌드 오류 0(경고 4=기존 노이즈) / 콘솔 에러 0 / EditMode 258개 전부 통과.
   ※ F 클립 pole 보정 소급 적용 여부(§10-2-1)는 여전히 미결 — 현행 F 유지 중.
5. **P4 — 인게임 활용 (B안 = 대쉬 교체)** ✅ 완료 (2026-08-05): 사양·구현 기록은
   **05 §10-B7**이 원본. 요지 — 축 4방 대쉬 8클립(지상/비행)의 내용을 방향별 히어로 포즈로
   교체(0~12f, f0=85%→f3=100% 홀드, aim2+pole), 액션명·FBX명·테이크명 유지로 Unity 측
   변경 0. 대각 대쉬(FL/BL/FR/BR)는 처음 기존 유지였다가 스타일 불일치로 **2026-08-05
   배선 롤백**(05 §10-B3) — 대각 입력은 인접 히어로 클립 블렌드. 대각 히어로화는 §10-2
   대각 포즈 결정과 함께.
   F 대쉬의 다리는 이등분 pole 보정판(좌 18/18/18·우 23/34/46 실측 재현).
   전시용 F 포즈 클립도 2026-08-05 같은 보정으로 소급 갱신 완료(§9-2) — 전 클립 일치.

### 10-4. 검증 (Definition of Done)

- 방향별 실루엣 3요소: ① 리드/트레일 다리 비대칭 ② 이동 방향 체중·뱅킹이 읽힘
  ③ **총구·시선은 전방 유지** (방향이 달라도 조준이 흔들리지 않는 것이 합격 기준).
- 무릎캡 방향 판정(§9-2 요령) 전 방향 통과 + 루프 봉합(f0=f60).
- CLAUDE.md §1 3단계 (빌드 0 / 콘솔 0 / 테스트 전부 통과).

### 10-5. 제자리 대기 히어로 포즈 — `Mecha_Pose_HeroIdle` ✅ (2026-08-05)

사용자 요청("제자리 서있을 때 포즈") → 시안 v1(뒷다리 역관절)을 자체 수정한 **v2 렌더 승인**
후 제작. 지상 사수 스탠스 — 런지 4종과 달리 **접지 상태**다.

**설계 원칙**: ① 총구·시선 = F 절대값 고정(오른팔 체인·Head) ② 왼발 앞/오른발(총 쪽) 뒤
스태거 + 무릎 살짝 굽힘 ③ 빈 왼팔은 몸에 붙여 자연 하강(v5 교훈) ④ **발은 aim2 제외**
(평발 접지는 롤이 자연스러워야 — aim2를 걸면 뒷발이 -62° 뒤틀림) ⑤ **사웨이는 상체·팔만**
(Hips·다리 완전 고정 = 발 미끄러짐 0, Hips z 사웨이도 0 — 부양 클립과 다른 점).

```
Hips  U   6   0   8 | Spine02 U  8  -1   5 | Spine01 U  9  -2   0 | Spine U 10  -3  -6
neck  U  14  -6 -12 | Head    U 20 -12 -26                          (시선 = F)
RightShoulder L -85   6 | RightArm L  -55 -62 | RightForeArm L    5 -22 | RightHand L  -12 -32   (= F, 총구 고정)
LeftShoulder  L  95   6 | LeftArm  L  115 -62 | LeftForeArm  L   95 -55 | LeftHand  L   90 -52   (자연 하강)
LeftUpLeg  L   10 -68 | LeftLeg  L  185 -84 | LeftFoot  L   10 -28 | LeftToeBase  L   10  -4   (앞발)
RightUpLeg L -168 -78 | RightLeg L -170 -66 | RightFoot L  -30 -32 | RightToeBase L  -30  -8   (뒷발 — el은 접지 정렬 보정값)
Hips location = (0, 0.01, -0.02)   / 다리 aim2 = UpLeg·Leg만, 이등분 pole (좌 -11/-8, 우 -11/-10)
```

- **뒷다리 요령**: 몸 뒤에 심은 다리는 정강이 el이 허벅지보다 **완만해야**(더 뒤로 쓸려야)
  무릎캡이 전방을 본다 — 트레일 다리 규칙(§9-2)과 동일. v1은 반대로 잡아 역관절이었다.
- **접지 정렬**: 양 발끝 월드 z 편차 2mm (뒷발 Foot el -25→-32 보정), f0=f30 동일 = 미끄러짐 0.
- 산출물: 액션(0~60, fake_user, fcurve 91) / `Rigged/Mecha_Anim_HeroIdle.fbx` / 클립 2.0s
  Human Loop (임포트 §4-확정 동일) / RigLab `_poseClips[4]` 배선. 렌더 `Idle_f0_*`·`Idle_unity.png`.
- 검증: 루프 봉합 OK / 무릎캡 양쪽 전방 / 빌드 0 / 콘솔 0 / EditMode 258 통과.
- **열린 항목**: 인게임 정지 상태(G1 Idle/F1 Hover)를 이 포즈 기반으로 교체할지는 미결 —
  착수 시 05 §10 백로그 형식으로 사양부터 (기존 Idle 루프 모션 대체라 범위가 다름).

